export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;

export const DEFAULT_BOOTSTRAP_WINDOW = 60;
export const MAX_BOOTSTRAP_WINDOW = 240;

export const ALLOWED_EXTENSIONS = new Set([".mp4", ".mov", ".avi", ".mkv"]);
export const ALLOWED_MIME_TYPES = new Set([
  "video/mp4",
  "video/quicktime",
  "video/x-msvideo",
  "video/x-matroska"
]);

export const REQUEST_ID_HEADER = "x-request-id";
